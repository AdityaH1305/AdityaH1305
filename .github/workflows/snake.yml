name: Generate Snake

on:
  schedule:
    # Runs at 00:00 UTC every day
    - cron: "0 0 * * *"
  workflow_dispatch: # Allows manual triggers
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. Essential: Checks out your repo so the workflow can save the SVGs
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Generates the snake files
      - name: Generate Snake
        uses: Platane/snk@v3
        with:
          github_user_name: Jonathan-Jesni
          # Overriding colors to force dark mode empty boxes (#161b22) and a purple snake
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=purple&color_dots=#161b22,#0e4429,#006d32,#26a641,#39d353
            dist/github-contribution-grid-snake-dark.svg?color_snake=purple&color_dots=#161b22,#0e4429,#006d32,#26a641,#39d353

      # 3. Pushes the files to the 'output' branch
      - name: Push to Output Branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
